const config = {
  siteUrl: 'https://www.luke516.blog',
  generateRobotsTxt: true, // (optional)
  // turn off index sitemap
  generateIndexSitemap: false,
  // ...other options
}
// TODO: internalized routes

module.exports = config
